= form_for @contract_application do |f|
  -if @contract_application.errors.any?
    #error_explanation
      %h2= "#{pluralize(@contract_application.errors.count, "error")} prohibited this contract_application from being saved:"
      %ul
        - @contract_application.errors.full_messages.each do |msg|
          %li= msg
  name:
  = f.text_field :name

  .actions
    = f.submit 'Save'
