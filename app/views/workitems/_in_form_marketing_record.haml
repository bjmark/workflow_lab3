指派给 : 
%select{:name =>"business_dept_id", :size=>"1"}
  %option{:value => ''}
  - @other_depts.each do |d|
    %option{:value => d.id, :selected=>(@workitem.fields['blade']['other_business_dept_id'].to_i == d.id)}= d.name

